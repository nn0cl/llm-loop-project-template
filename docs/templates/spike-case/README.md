# Spike case template

Copy this directory when opening a spike:

```bash
# from repository root
n=0001
slug=short-description
cp -R docs/templates/spike-case "docs/spike/case-${n}-${slug}"
# then edit case.md metadata (Case ID, Title, Status)
```

Rules: `docs/spike/README.md`.
